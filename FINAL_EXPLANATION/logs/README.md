# 🧾 logs — Loose Runtime Logs

> Part of [`FINAL_EXPLANATION/`](../README.md) · 📤 **generated artifacts**, not required to
> rerun anything.

Loose runtime logs are collected here so the root folder stays focused on source files and
documentation. Useful for checking previous visualizer sessions or debugging prior runs.

Per-shard logs from multi-GPU runs remain inside the relevant output directory:

```text
outputs/<run_name>/logs/
outputs/<run_name>/validation_logs/
```

---

⬆️ Back to [`FINAL_EXPLANATION/`](../README.md)
