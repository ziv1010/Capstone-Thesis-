# embedding_analysis

Final embedding-analysis snapshots grouped by thesis draft.

The active analysis scripts are in:

```text
section_GNN/embedding_analysis/
FINAL_EXPLANATION/
```

Subfolders such as `draft_2/` and `old_draft_1/` contain copied results for
writing and comparison.
