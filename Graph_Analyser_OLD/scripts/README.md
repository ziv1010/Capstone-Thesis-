# scripts

Legacy command-line phases for the old graph analyser.

The scripts are named by analysis phase, including inference/indexing,
explainer training, importance extraction, misclassification diagnostics, and
counterfactual embedding analysis.

Use `scripts/run_all.sh` only when deliberately rerunning the old analyser.
Current explanation workflows live in `FINAL_EXPLANATION/run_scripts/`.
