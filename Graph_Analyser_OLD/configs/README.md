# configs

Legacy graph-analyser configuration files.

The configs here belong to `Graph_Analyser_OLD` and are retained for older
analysis runs. They are not used by the current `section_GNN` or
`FINAL_EXPLANATION` workflows.
