# Environment: model_comparison_inlegalllama
# Purpose: model comparison experiments for InLegalLLaMA / FactLegalLLaMA.
# Source basis: model comparison/environment.yml.

micromamba env create -f "model comparison/environment.yml"

# channels:
conda-forge

# conda dependencies:
python=3.11
pip

# pip dependencies:
torch==2.8.0
transformers==4.57.1
accelerate==1.11.0
safetensors>=0.7.0
huggingface_hub>=0.36.2
tokenizers>=0.22.0,<0.23.0
tqdm>=4.67.0
sentencepiece>=0.2.0
protobuf>=6.0.0
peft>=0.13.0
