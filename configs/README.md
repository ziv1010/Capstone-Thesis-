# configs

Root-level legacy configuration files.

Most active pipeline configuration now lives beside the scripts that consume it:

- `Fixed_GPU_OpenNyai/`
- `DATA_SET_BUILDER_AND_EXPLORER/Timeline_Maker/`
- `section_GNN/`
- `FINAL_EXPLANATION/`

Keep this folder only for shared or older configs that are still useful as
reference. New experiment configs should generally be placed inside the relevant
pipeline folder.
