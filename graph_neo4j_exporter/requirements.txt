# core runtime
torch>=2.0.0

# Neo4j Python driver (official — no py2neo needed)
neo4j>=5.0.0

# torch-geometric is NOT required here — we only torch.load the saved bundle
# If the bundle uses PyG HeteroData objects, torch alone is sufficient.
